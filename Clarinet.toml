[project]
name = 'Bustoken'
description = ''
authors = []
telemetry = true
cache_dir = './.cache'
requirements = []
[contracts.ride-tracker]
path = 'contracts/ride-tracker.clar'
clarity_version = 3
epoch = 'latest'

[contracts.ticket-manager]
path = 'contracts/ticket-manager.clar'
clarity_version = 3
epoch = '3.2'
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false

[repl.remote_data]
enabled = false
api_url = 'https://api.hiro.so'
